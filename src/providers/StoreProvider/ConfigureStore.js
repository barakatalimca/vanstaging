// // import { createStore, persist } from 'easy-peasy'
// import localForage from 'localforage'

// import model from '../../models'

// export default createStore(persist(model, { storage: localForage }), { name: 'easystore' })
